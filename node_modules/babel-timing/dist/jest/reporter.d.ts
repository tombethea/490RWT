import { render } from '../index';
import { Reporter } from '@jest/reporters';
import { Config } from '@jest/types';
declare type Options = Parameters<typeof render>[1];
export default class BabelTimingReporter implements Reporter {
    private _options;
    constructor(globalConfig: Config.GlobalConfig, { output, ...otherOptions }: Options);
    onRunComplete(): void;
    onRunStart(): void;
    getLastError(): void;
}
export {};
