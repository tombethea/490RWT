import { ResultByFile, ResultByPlugin, Options } from '../types';
export default function render(results: ResultByFile[] | undefined, options: Pick<Options, 'expandPackages' | 'output' | 'outputPath' | 'aggregateBy' | 'paginationSize'>): ResultByFile[] | ResultByPlugin[] | undefined;
