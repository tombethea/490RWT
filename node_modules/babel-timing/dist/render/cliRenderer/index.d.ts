import { ResultByFile, ResultByPlugin } from '../../types';
export default function renderer(results: ResultByFile[] | ResultByPlugin[] | undefined, { paginationSize }: {
    paginationSize: number;
}): void;
